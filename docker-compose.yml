version: "3.9"

services:
  frontend:
    build: .
    container_name: frontend-doce-dondocas
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules: